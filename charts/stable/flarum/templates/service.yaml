apiVersion: v1
kind: Service
metadata:
  name: {{ include "flarum.fullname" . }}
  labels: {{- include "flarum.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range $name, $port := .Values.service.ports }}
    - port: {{ $port }}
      targetPort: {{ $port }}
      protocol: TCP
      name: {{ $name }}
    {{- end }}
  selector: {{- include "flarum.selectorLabels" . | nindent 4 }}