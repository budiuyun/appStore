apiVersion: v1
kind: Service
metadata:
  name: {{ include "python-vscode.fullname" . }}
  labels: {{- include "python-vscode.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range .Values.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .port }}
      protocol: {{ .protocol }}
      name: {{ .name }}
    {{- end }}
  selector: {{- include "python-vscode.selectorLabels" . | nindent 4 }} 