apiVersion: v1
kind: Service
metadata:
  name: {{ include "ubuntu-ssh.fullname" . }}
  labels: {{- include "ubuntu-ssh.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range $name, $port := .Values.service.ports }}
    - port: {{ $port }}
      targetPort: {{ $port }}
      protocol: TCP
      name: {{ $name }}
    {{- end }}
  selector: {{- include "ubuntu-ssh.selectorLabels" . | nindent 4 }}