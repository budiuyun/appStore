apiVersion: v1
kind: Service
metadata:
  name: {{ include "windows.fullname" . }}
  labels:
    {{- include "windows.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range $name, $port := .Values.service.ports }}
    - port: {{ $port }}
      targetPort: {{ $name }}
      protocol: TCP
      name: {{ $name }}
    {{- end }}
  selector:
    {{- include "windows.selectorLabels" . | nindent 4 }}