apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-vscode.fullname" . }}
  labels:
    {{- include "go-vscode.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range .Values.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .port }}
      protocol: {{ .protocol }}
      name: {{ .name }}
    {{- end }}
  selector:
    {{- include "go-vscode.selectorLabels" . | nindent 4 }} 