apiVersion: v1
kind: Service
metadata:
  name: {{ include "mcsm-web.fullname" . }}
  labels: {{- include "mcsm-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range .Values.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .port }}
      protocol: {{ .protocol }}
      name: {{ .name }}
    {{- end }}
  selector: {{- include "mcsm-web.selectorLabels" . | nindent 4 }}