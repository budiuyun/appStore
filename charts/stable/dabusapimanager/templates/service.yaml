apiVersion: v1
kind: Service
metadata:
  name: {{ include "dabusapimanager.fullname" . }}
  labels: {{- include "dabusapimanager.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range .Values.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .port }}
      protocol: {{ .protocol }}
      name: {{ .name }}
    {{- end }}
  selector: {{- include "dabusapimanager.selectorLabels" . | nindent 4 }}