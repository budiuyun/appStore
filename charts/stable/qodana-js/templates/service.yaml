apiVersion: v1
kind: Service
metadata:
  name: {{ include "qodana-js.fullname" . }}
  labels: {{- include "qodana-js.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range $name, $port := .Values.service.ports }}
    - port: {{ $port }}
      targetPort: {{ $port }}
      protocol: TCP
      name: {{ $name }}
    {{- end }}
  selector: {{- include "qodana-js.selectorLabels" . | nindent 4 }}