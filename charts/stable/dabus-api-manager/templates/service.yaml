apiVersion: v1
kind: Service
metadata:
  name: {{ include "dabus-api-manager.fullname" . }}
  labels: {{- include "dabus-api-manager.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range .Values.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .port }}
      protocol: {{ .protocol }}
      name: {{ .name }}
    {{- end }}
  selector: {{- include "dabus-api-manager.selectorLabels" . | nindent 4 }}