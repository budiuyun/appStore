apiVersion: v1
kind: Service
metadata:
  name: {{ include "api-management.fullname" . }}
  labels: {{- include "api-management.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range .Values.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .port }}
      protocol: {{ .protocol }}
      name: {{ .name }}
    {{- end }}
  selector: {{- include "api-management.selectorLabels" . | nindent 4 }}