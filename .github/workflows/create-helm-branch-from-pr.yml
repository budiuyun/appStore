# 此工作流已被禁用 - 取代为 pr-to-helm.yml
# 此文件保留仅供参考

name: DISABLED - Create Helm Branch From PR

# 完全禁用触发器
on:
  # 仅手动触发，且需禁用
  workflow_dispatch:
    inputs:
      disabled:
        description: '此工作流已禁用'
        required: true
        default: 'DISABLED'

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: 这个工作流已被禁用
        run: |
          echo "此工作流已被禁用，请使用 pr-to-helm.yml 工作流"
          exit 1 